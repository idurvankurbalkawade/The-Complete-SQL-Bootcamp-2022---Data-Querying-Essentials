SELECT CURDATE(),
	   CURRENT_DATE(),
       CURTIME(),
       CURRENT_TIME(),
       NOW(),
       CURRENT_TIMESTAMP(),
       SYSDATE(),
       SLEEP(5),
       NOW(),
       SYSDATE()

FROM rental
LIMIT 1;